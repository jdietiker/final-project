### The required Graphics and Csv libraries can be installed with the following commands: ###
```
opam install graphics
```
opam install csv
```
### Mac OS X ###
On Mac, a separate server is required to open the GUI window:
```
https://www.xquartz.org/
```